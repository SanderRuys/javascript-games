import { cardsArray } from "./cards.js";

/*const main = document.querySelector("main");

// display card function
const displayCard = (name, img, alt) =>{
    let card = document.createElement("img");
    card.src = img;
    card.alt = alt;
    card.addEventListener("click", alert(name + "geclicked"));
    main.append(card);
}

for (let i = 0; i < cardsArray.length; i++) {
    displayCard(cardsArray[i].img, cardsArray[i].alt );
    
}*/